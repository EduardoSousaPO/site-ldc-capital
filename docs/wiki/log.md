# Wiki Log — site-ldc

> Histórico cronológico vivo. Tipos: `INGEST`, `SETUP`, `RELEASE`, `BUGFIX`, `INCIDENT`, `MVP`, `ADR`, `DECISION`.
> ≤ 200 linhas. Arquivar acima disso.

## 2026-05-20

- `SETUP` — Reorganização Harness v3.2 iniciada. Fase 1 (espinha mínima v3.2) criou: `AGENTS.md`, `CLAUDE.md`, `TODO.md` (raiz), `docs/{product,specs,contracts,plans,decisions/adr,wiki}/`, `docs/plans/{DECISIONS_LOG,risk-classification,FEATURE-CONTRACT-template,VALIDATION-MATRIX-template}.md`, `docs/plans/feature-contracts/.gitkeep`, `packages/shared/types/.gitkeep`, `tests/{unit,integration,contract,e2e}/.gitkeep`, `.github/workflows/ci.yml` (N1 ativo), `docs/wiki/{index,log,overview,architecture}.md` (stubs).
- `DECISION` — Adoção gradual (Cenário C §17). Zero refactor preventivo, zero migration preventiva. Próximas features migram sob demanda.
- `INGEST` — Fase 2 produziu `docs/plans/CURRENT_REALITY.md` (338→~360 linhas). Diagnóstico revelou: (a) **3 bugs de segurança em rotas admin (BUG-002 P0 setup-admin, BUG-003 P1 add-users, BUG-004 P2 media)** — todos registrados em `TODO.md §5`, nenhum corrigido nesta sessão; (b) **8 tabelas Supabase em produção sem migration versionada** (`BlogPost`, `Material`, `Category`, `MaterialCategory`, `BlogPostApprovalToken`, `carousel_runs`, `ebook_leads`, wealth-planning tables) — risco de drift staging/prod, item C no backlog para versionar como SQL idempotente; (c) confirmação F-019 carrossel em produção desde 2026-05-09 (ADR-006); (d) confirmação `role` de admin vive em `user_metadata.role` do Supabase Auth (não em tabela separada). Detalhes em `TODO.md §5` + `CURRENT_REALITY.md §9` + §10 (status [RESOLVIDO]/[VIROU BUG]/[PERMANECE]).
- `BUGFIX` — **BUG-002 P0** — `/api/setup-admin/route.ts` deletado (rota + pasta) em commit `27d87a3`. Rota expunha criação de admin SEM auth com senha hardcoded `admin123` retornada no JSON. Seed admin permanece coberto por `scripts/sync-admin-users.ts` (4 admins, senha do `ADMIN_SYNC_PASSWORD`/`ADMIN_PASSWORD` env, smoke test). Classe D, modo fast-fix justificado (deleção de superfície, sem migration/env/deploy). Validação pós-deploy: `curl -i -X POST https://ldccapital.com.br/api/setup-admin` → 404 confirmado em 2026-05-20.
- `INGEST` — Fase 3 (Wiki bootstrapping) resolveu 28 `[VERIFICAR]` em lote. Achados novos: (a) **2 tabelas órfãs adicionais** — `User` (espelho de `user_metadata.role` via UPSERT em `auth-check.ts:36-47`) e `Client` (usada por `/api/lead` no lugar de `Lead` que "não existe" segundo comentário em `route.ts:41`). Total agora **10 tabelas órfãs**. (b) **2 novos bugs (BUG-005 P2, BUG-006 P3)** — ausência de rate limiting em `/api/dividend-tax/report` (Playwright caro) e em endpoints públicos de lead capture. (c) **`/api/pgbl-simulator/pdf` retorna HTML, não PDF** — endpoint mal nomeado; renomeação no backlog A. (d) `checkAdminAuth` aceita `ADMIN` e `EDITOR` (rejeita `USER` em `auth-check.ts:65-67`). (e) `/blog/[slug]` é render dinâmico sem ISR. (f) Sitemap regenerado a cada request (sem `revalidate`). 3 decisões registradas em `DECISIONS_LOG.md`. Wiki módulos `auth-admin`, `blog-cms`, `lead-capture`, `pgbl-simulator`, `seo-sitemap`, `wealth-planning` atualizados.
- `INGEST` — Fase 6c.3 concluída: 10 docs legacy de `docs/` raiz classificadas (4 archive + 3 módulos Wiki + 3 runbooks Wiki). `docs/_archive/` criado com README de propósito. `docs/` raiz limpo (zero `.md` solto). Novos módulos: `components-radix`, `design-system`, `assets-catalog`. Novos runbooks: `admin-panel-uso`, `seo-visibilidade`, `google-sheets-setup` (3 excedem ≤150 linhas — backlog A para split). `wiki/index.md` atualizado com 6 novos itens + ponteiro para `_archive/`. Refs cruzadas em PRD, `wiki/modules/{admin-panel,blog-cms,materiais,seo-sitemap}.md` e `GUIA_CALCULADORA_TRIBUTACAO_DIVIDENDOS.md` atualizadas. BUG-007 (`/api/pgbl-simulator/pdf` sem Zod) adicionado ao TODO §5.
- `SETUP` — **Reorganização Harness v3.2 concluída em 2026-05-21.** 11 fases executadas em sessão única: Fase 1 (espinha mínima 19 arquivos), Fase 2 (CURRENT_REALITY snapshot do código), Fase 3 (Wiki bootstrap — overview/architecture/log/10 módulos), Fase 4 (6 runbooks), Fase 6a (PRD raiz), Fase 6b (migração `docs/news/` → estrutura unificada — 13 git mv + 1 git rm), Fase 6c.1 (SPEC raiz com Anti-SPEC §6 sagrada), Fase 6c.2 (CONTRACTS raiz catálogo), Fase 6c.3 (10 docs legacy classificadas — 4 archive + 6 Wiki), Passos finais 1-3 (auditoria AGENTS+CLAUDE + cursor-brief template + wiki lint). **Estrutura final:** 1 PRD + 1 SPEC raiz + 1 anexo `spec-pipeline-ia.md` + 1 CONTRACTS raiz + 1 anexo `contracts-pipeline-ia.md` + 7 ADRs + 13 módulos Wiki + 9 runbooks + 4 archives + 5 templates (`FEATURE-CONTRACT`, `VALIDATION-MATRIX`, `cursor-brief`, `risk-classification`, `DECISIONS_LOG`) + CI N1 ativo + AGENTS.md + CLAUDE.md + TODO.md raiz. **Zero edits em `src/`, `public/`, `supabase/`, `content/`, `scripts/`, configs.** **Bugs abertos:** 6 (BUG-001 credencial Google, BUG-003 add-users sem auth, BUG-004 media sem auth, BUG-005 dividend-tax/report sem rate limit, BUG-006 lead endpoints sem rate limit, BUG-007 pgbl/pdf sem Zod). **BUG-002 RESOLVIDO** em 27d87a3 (sessão paralela 2026-05-20). **ADRs candidatos pendentes:** ADR-008 (política IP), ADR-009 (role `user_metadata` vs tabela User), ADR-009b (schema versionada das 10 órfãs), ADR-010 (LGPD wealth planning). **Próximos passos:** (a) Eduardo revisa diff + decide commit; (b) BUG-001 (rotação Google service account) via runbook `secrets-rotation.md`; (c) BUG-003/004/005/006/007 atendidos sob demanda em features futuras conforme prioridade; (d) janela UTM separada (PR `feat/utm-tracking-youtube-diagnostico → main`); (e) próxima feature C/D segue Harness v3.2 (Prompt 1 → Feature Contract → Prompt 2 → matriz → Prompt 3).
